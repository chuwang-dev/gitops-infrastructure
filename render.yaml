services:
  - type: web
    name: gitops-sample-webserver
    runtime: image
    imageURL: docker.io/library/nginx:alpine
    plan: free
    envVars:
      - key: PORT
        value: "10000"
      - key: NGINX_PORT
        value: "10000"